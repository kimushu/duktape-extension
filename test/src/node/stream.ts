/*import * as stream from "stream";

describe("stream", () => {
    it("exists", () => {
        assert.exists(stream);
    });
    it("has circular reference", () => {
        assert.strictEqual(stream, stream.Stream);
    });
});
*/